---
---
Created `tractor-fan-in-proof` with verified copies `left.txt` and `right.txt`. Confirmed branch isolation and overlapping intervals (`1786974899–1786974903`). `summary.txt` contains exactly:

isolation=verified
overlap=verified

Neither branch worktree was altered.

