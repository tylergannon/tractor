---
---
First-round fix, no EVAL_FEEDBACK.md present.

Root cause: the error "declaring permissions: cortex tool write_to_file: ... is not a valid artifact path; artifacts must be in ~/.gemini/antigravity-cli/brain/<uuid>/" is not produced anywhere in this repo (confirmed via grep) — it's the external agy CLI's own error text for a non-SUCCESS result envelope, passed through unchanged by harness/agy/adapter.go's categorize(). Evidence from both run reproductions (shared: 41348176..., isolated: 75e199e9...) shows agy's native write_to_file tool physically writes the declared artifact file to the workspace (visible tool_result with no error, and the file is confirmed present on disk in the isolated worktree), but agy's own end-of-turn "declaring permissions" bookkeeping rejects the write path (must be under its private brain/<uuid>/ directory) and fails the whole turn ~12ms after the visible assistant text, discarding the turn's structured output along with it. This is not bisectable to a single regressing commit in this repo: harness/agy/*.go is unchanged since eefed09/5eb89fc, and engine/artifacts.go (added in 2485e4c) never injects branch.Artifacts into any prompt — it's a pure post-hoc filesystem collector, skipped once result.Error != nil. The PR #18-era proof pipeline never actually exercised a gemini/agy branch with a declared artifact (only openai+anthropic), so this failure mode was never proven to work before wisdom-circle.

Fix: harness/agy/adapter.go RunTurn now detects this specific agy failure signature (isArtifactPathError, matching stable substring "is not a valid artifact path") and spends its one bounded repair attempt (mirroring the existing schema-repair mechanism) retrying the turn once with a corrective instruction telling the model to redo the work via its shell/terminal tool instead of the restricted native write tool. Bounded to exactly one retry — no infinite loop, no silent/faked success; a second failure still propagates the terminal error. Correct for both workspace policies because it operates purely inside the agy turn-execution loop, before engine/artifacts.go (which is workdir/policy-aware) ever runs.

Regression tests added to harness/agy/adapter_test.go in the existing fake-subprocess table-driven style: TestIsArtifactPathError (unit test on detector against real error text from both repros), TestRunTurnRecoversFromArtifactPathError (fake agy fails once then succeeds after repair prompt — asserts 2 invocations, valid result, repair prompt contains both guidance and original error), TestRunTurnArtifactPathErrorRepairsAtMostOnce (fake agy always fails — asserts exactly 2 invocations, bounded retry, terminal error still returned).

Verified fails-without-fix by `git stash push -- harness/agy/adapter.go` (keeping new tests, temporarily removing only TestIsArtifactPathError since it wouldn't compile without the new helper), ran the two RunTurn-level tests and captured FAIL output (single invocation, raw agy error returned, no repair). Restored via `git stash pop` and re-added TestIsArtifactPathError. Full suite then re-verified: gofmt -l . clean, go vet ./... clean, go test ./... all pass (11 packages, all ok).

Deliverables written: harness/agy/adapter.go (fix), harness/agy/adapter_test.go (tests), WORK_SUMMARY.md (full root cause, fix rationale, both workspace-policy correctness argument, fails/passes test evidence, gofmt/vet/test output) — all in workspace root of this worktree. Nothing under /Users/tyler/src/tractor (main checkout) or antigravity-integration-plan was touched. No real agy/Gemini invocations were made. No commits created, working tree left as-is per instructions.