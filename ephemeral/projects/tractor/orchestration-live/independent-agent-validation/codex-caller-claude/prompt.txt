Read inventory.tsv in the current workspace. Reconcile every row using variance_units = observed_units - expected_units and impact_cents = variance_units * unit_cost_cents. Write inventory_audit.json in the workspace as valid JSON with exactly these top-level keys: dataset_id, row_count, net_variance_units, net_impact_cents, locations, discrepancies. locations must be an object keyed by location, each value containing expected_units, observed_units, variance_units, and impact_cents aggregated for that location. discrepancies must contain only nonzero-variance rows, sorted by descending absolute impact_cents and then sku ascending; each entry must contain location, sku, variance_units, and impact_cents. Do not modify inventory.tsv. Re-read the JSON and verify all totals against the TSV before finishing. This is a real workspace task: use your public file/shell tools rather than guessing. Return normally with next set to done and notes briefly naming the artifact and grand totals.
