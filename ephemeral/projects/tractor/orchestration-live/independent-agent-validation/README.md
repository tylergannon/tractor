# Independent agent validation evidence

- `report.md`: verdict and claim-by-claim evidence assessment.
- `verification.txt`: concise final checks.
- `agent` and `agent-binary.sha256`: the one-time build and digest.
- `codex-caller-claude/`: fresh nonce-bearing inventory input, exact prompt/invocation, public logs, native-session extract, stdout/exit status, independently computed expected JSON, and produced artifact.
- `claude-caller-codex/`: fresh nonce-bearing ledger inputs, exact prompt/invocation, public logs, native-session extract, stdout/exit status, independently computed expected JSON, and produced artifact.

The built binary was invoked exactly twice. No tests were used as validation evidence.
