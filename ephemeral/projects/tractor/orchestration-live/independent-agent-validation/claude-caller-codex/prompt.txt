Read accounts.tsv and transactions.tsv in the current workspace. Reconcile the ledger by summing transaction delta_cents per account and computing calculated_closing_cents = opening_cents + transaction_total_cents and difference_cents = calculated_closing_cents - declared_closing_cents. Write ledger_reconciliation.json in the workspace as valid JSON with exactly these top-level keys: dataset_id, transaction_count, accounts, discrepancy_accounts, total_difference_cents, total_absolute_difference_cents, all_declared_balances_match. accounts must be an array sorted by account ascending, with each entry containing account, opening_cents, transaction_total_cents, calculated_closing_cents, declared_closing_cents, and difference_cents. discrepancy_accounts must list only accounts with nonzero difference_cents, sorted by descending absolute difference_cents then account ascending. Reject any transaction whose dataset_id differs or whose account is absent, but this dataset should reconcile without rejected rows. Do not modify either input. Re-read the JSON and independently verify its totals before finishing. This is a real workspace task: use your public file/shell tools rather than guessing. Return normally with next set to done and notes briefly naming the artifact and aggregate discrepancy.
