Inspect `inventory.json` in the workspace. For every inventory row, compute quantity multiplied by unit_cents and group those extended values by warehouse. Write `audit.json` with exactly these fields: `source_nonce` copied from the input; `warehouses`, an array sorted by warehouse name containing objects with `warehouse`, `line_count`, and `total_cents`; `grand_total_cents`; and `largest_warehouse`, the warehouse with the highest total (break a tie by alphabetically earliest name). Verify your arithmetic from the source file before finishing. Return `next` as `done` and use `notes` for a concise result summary.
