decision: Update the website's pipeline guide and specification comparison for structured heterogeneous fan-out, declared artifacts, isolated-by-default workspaces, and explicit shared workspaces.
decision: Keep `.agents/plugins/marketplace.json` unchanged because its Tractor source already tracks this repository's `main`; the installable release version belongs in `.codex-plugin/plugin.json`.
friction: The requested plugin version was not specified. The current manifest is `0.3.0`, and the only post-release change is a backward-compatible feature, so `0.4.0` is recommended but release state must wait for user confirmation.
correction: The user explicitly said to stop asking for release-version confirmation and take care of the release -> use the natural backward-compatible feature bump `0.4.0` and carry it through the full repository release workflow.
friction: The first `vp run verify` could not plan because the fresh worktree had no frontend dependencies and therefore no `rsvelte-fmt` binary -> install the lockfile-pinned dependencies with `vp install --frozen-lockfile` before site verification.
verification: After formatting the two edited Markdown pages, `vp run verify` passed all seven stages: repository formatting/lint, rsvelte formatting/lint/check, Astro diagnostics/build, and preview verification of three routes plus four GitHub Pages-safe assets.
verification: Release checks passed: `go test -race -count=1 ./...`, `go vet ./...`, `golangci-lint run ./...`, plugin validation, release-skill validation, and `git diff --check`.
proof: A fresh build from the `0.4.0` release tree completed the heterogeneous OpenAI/Anthropic pipeline in a disposable initialized Git workspace. Machine assertions verified isolated worktrees, inherited `openai` / `gpt-5.6-sol` / `low`, overridden `anthropic` / `claude-sonnet-4-5` / `medium`, exact collected artifacts, fan-in paths and contents, and manifest version `0.4.0`.
