# Spec and supervisors migration

decision: The live authority for this migration is `tylergannon/attractor` `origin/main` at `0aca8b748e6ecc23446fc690d2b66690b77fe0d3`; the document was read in full before design.
decision: Preserve YAML as a Tractor input extension, but validate and decode it through the same generated schema as JSON.
correction: Designs for this project must stay skeletal: decisions, ownership, and sequencing only; no code or pseudo-code.
decision: Implement the new pipeline language directly without a compatibility layer for the removed `start` and `exit` node types or the old shared routing representation.
friction: The upstream specification is a 1,100-line insertion and 1,379-line deletion relative to Tractor's pinned copy, so heading-level comparison is insufficient; conformance must be derived from the full current document and implementation inspection.
decision: Record upstream specification defects and ambiguities as findings with section references; do not repair or reinterpret the authority document during the Tractor implementation.
decision: Replace Tractor's stale normative `docs/spec.md` byte-for-byte with upstream `0aca8b7`; preserve YAML as an explicitly requested Tractor extension rather than editing it into the upstream authority.
decision: Extend the backend seam with a binding-open callback so supervisor bindings are checkpointed before dispatch. Treat first briefing delivery as idempotent and at-least-once across crashes, because the upstream exact-once wording is not implementable without transactional harness acknowledgement; record the exact binding in `briefed.json` only after a successful turn, and resend for missing, changed, or inconclusive binding evidence.
decision: Preserve the normative supervisor verdict schema as the engine and validation authority. At the Codex adapter boundary only, present optional root properties as required nullable fields to satisfy Codex strict-output rules, remove null placeholders for originally optional fields, then validate against the unchanged caller schema; Claude receives the schema unchanged.
friction: A live conformance run exposed a typed-nil mistake in the new scenario wrapper: converting a nil `*harness.Error` directly to `error` produced a non-nil interface with a blank message. Return an explicit nil interface after successful backend calls.
proof: Full native conformance, including exact supervisor `ok` then `steer` on one reused session and two run-log segments, passed with Codex CLI 0.147.0 and Claude Code 2.1.233; the eight-scenario reports are preserved in `ephemeral/projects/tractor/conformance/`.
proof: The shipped cross-harness supervisor pipeline completed with Claude supervisor verdicts delivered to a live Codex worker; the deterministic verifier accepted `TRACTOR_SUPERVISOR_RECEIVED`, the first patrol rotated and tallied a completed digest, and `current.jsonl` pointed to the index during overlap. Preserved evidence is in `ephemeral/projects/tractor/spec-supervisors-proof/live-steering/`.
proof: The standalone `agent` binary performed and independently re-read non-trivial workspace tasks through Codex and Claude; exact byte comparisons passed, with results preserved in `ephemeral/projects/tractor/spec-supervisors-proof/agent/`.
proof: The migrated YAML, external-steering, and parallel fan-out/fan-in examples completed through the built `tractor` binary; their deterministic workspace checks passed, steering was audited, and the parallel summary records isolation and overlap.
proof: A live interrupted run saved its supervisor binding and first batch, then resumed with the same binding, rotated the error backlog into batch 2, retried the work successfully, and did not resend the briefing. A separate live hierarchy carried verdict digests upward and coaching downward, and an operator stop interrupted an active supervisor turn and preserved its reason. Evidence and reconciliation are in `ephemeral/projects/tractor/spec-supervisors-proof/`.
