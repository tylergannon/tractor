decision: Preserve legacy string branch semantics byte-for-byte; the heterogeneous branch syntax must be additive and must resolve through the existing Codergen execution and validation path.
decision: Treat `isolated` as the implicit workspace policy so omitted policy remains behaviorally identical to existing fan-outs.
friction: The delegated brief defines inheritance semantics but not the concrete JSON/YAML field shape -> requested the coordinator's approved wire shape before changing the public generated schema.
decision: Use structured parallel branches with `id`, non-empty `artifacts`, and a fully optional `codergen` override. Resolve each override into an ordinary strict Codergen node before linting and execution; do not change standalone Codergen requirements.
decision: Record graph-level resolved branches in `resolved-branches.json`, runtime-validated branch settings in each synthesized branch stage's `resolved.json`, and collected artifact paths in `branches.json` so fan-in and later inspection share one evidence surface.
decision: In isolated mode, copy declared regular-file or directory artifacts into the parallel stage before deleting branch worktrees. In shared mode, record the declared paths in the common workspace and provide no conflict protection.
friction: The desktop environment continued to enforce workspace-only restrictions after the task was declared Full Access -> used task-local Go caches; the live CLI proof remained blocked because Unix control-socket binding is denied before a run starts.
verification: Generated schema is current; focused graph, lint, and parallel-engine tests passed, including heterogeneous inheritance, isolated collection, shared-workspace fan-in, and legacy behavior. `go vet ./...`, `golangci-lint run ./...`, and `git diff --check` passed.
verification: `go test -race -count=1 ./...` passed with unrestricted Unix-socket access.
proof: A fresh build of the real `tractor` CLI completed `examples/parallel/fan-out-fan-in.json` in a disposable initialized Git workspace. `branches.json` recorded distinct branch worktrees and stage directories, the branch intervals overlapped, fan-in copied both exact records, and the deterministic verifier accepted the exact `isolation=verified` / `overlap=verified` summary.
proof: The heterogeneous pipeline completed in a fresh initialized Git workspace. `resolved-branches.json` and per-branch `resolved.json` showed inherited `openai` / `gpt-5.6-sol` / `low` settings for `openai_branch` and overridden `anthropic` / `claude-sonnet-4-5` / `medium` settings for `anthropic_branch`; `branches.json` exposed both declared collected paths, the collected files had the exact requested newline-terminated contents, and fan-in reported both paths and contents.
proof: The shared-workspace pipeline completed in a fresh disposable workspace. Both resolved branch records used the same workspace, preserved the inherited and overridden provider/model settings, recorded each declared artifact at its shared path, produced exact `shared-left\n` and `shared-right\n` files, and fan-in verified both paths byte-for-byte.
