decision: YAML is a temporary syntax frontend over the generated JSON decoder. Use go.yaml.in/yaml/v4 to load one document into ordinary Go values, normalize with encoding/json, and keep graph.Parse as the single semantic authority until go-gen-jsonschema generates YAML decoding.
decision: Support .yaml and .yml files plus an explicit --yaml flag; keep --json unchanged and mutually exclusive.
decision: Prove the authoring benefit with a zero-token tool workflow using YAML comments and a literal block scalar.
