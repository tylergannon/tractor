# Orchestration implementation worklog

decision: `docs/spec.md` from `tylergannon/attractor` main is the orchestration authority; the existing harness/backend implementation is the completed lower layer.
doc_bug: the repository carried the prior 2,481-line DOT/chooser spec while the requested authority is now a 3,494-line typed-JSON/deterministic-routing spec -> replaced the local copy verbatim before planning.
friction: the local `~/src/attractor` checkout was two commits behind remote `main`, so its 3,494-line intermediate JSON draft was not authoritative -> fetched only the remote ref and replaced `docs/spec.md` again with the current 2,811-line nested-edge, chooser-routing spec at blob `7cd84cd`; always verify the named remote branch before copying.
doc_bug: the current sibling `cmd/attractor` parses arguments manually and imports no Cobra despite Cobra being present indirectly -> use the user's explicit Cobra requirement, not the stale reference CLI implementation.
correction: planning artifacts must stay high-level and contain no code; they describe decisions, sequencing, review boundaries, and proof only.
decision: use the sibling implementation for Cobra and generated-schema lessons, not as a source tree to port; every local choice must follow the revised spec and this repo's existing backend contract.
decision: current-spec orchestration requires a narrow backend reconciliation before engine work: stale-workdir bindings reopen for replayed fan-outs, event sequence resumes above existing logs, and `current.jsonl` stays on the index through genuine overlap.
decision: use `tylergannon/go-gen-jsonschema` as the sole structural schema/validation source; add only a duplicate-member preflight instead of a second hand-written decoder.
decision: the CLI input contract is explicit: file positional argument or `--json` string; no content/path guessing.
decision: implementation validation groups adjacent slices into Fable review loops, with a final unconditional implementation-and-proof review; completion audits all of spec 11.1 through 11.14.
decision: the skeletal 14-slice plan reached GPT consensus after one material repair round; round two found only a stale count in the closing proof sentence.
friction: `go-gen-jsonschema` v0.11.3 has no open discriminator fallback, regex constraints, or opaque-object schema -> keep generated structs and union branches authoritative, then deterministically normalize only the custom catch-all and pattern constraints; do not add a second field-admission decoder.
decision: for parallel linting, a cyclic branch is convergent only when every node reachable before the first shared fan-in retains a path to that fan-in; closed cycles fail `parallel_fan_in`.
decision: represent each branch interior without its fan-in; use the shared fan-in only for convergence, and treat it as outside the branch for workdir/thread-boundary rules.
doc_bug: Section 7.3 shows context-free validation, but `type_known`, `fidelity_valid`, and `thread_harness_consistent` require active runtime configuration -> expose a configured validator and keep the convenience API bound to built-in defaults.
decision: a direct parallel-to-fan-in edge is an empty branch and fails `fan_in_entry`, because the parallel node is not branch interior under Sections 4.8 and 7.2.
decision: `prompt_on_llm_nodes` follows the literal authored-field rule; an explicitly present empty `prompt` or `label` counts as present, while runtime fallback behavior remains separate.
decision: the first engine slice is a single-threaded registry and runner over the existing store; validation is a required injected function so runtime initialization cannot bypass lint accidentally, while tests can deliberately supply a no-op to exercise unlinted terminal guards.
decision: concrete retries, resume, parallel fan-in routing, and tool/codergen handlers stay out of the core-loop slice; the engine still records one attempt and one stage per current dispatch so those additions extend the state machine without a second execution path.
decision: the codergen handler resolves every turn field before backend handoff, owns only prompt/response artifacts and choice-schema construction, and keeps simulation behind a nil backend; registry wiring and fan-in evidence remain separate slices.
decision: choice-schema JSON uses fixed structs so property order, ordered route enums, strict required fields, and `additionalProperties: false` are directly testable without introducing a schema-building abstraction.
decision: the specification defines BranchResult contents but not its JSON member names; use direct `branch_id`, `outcome`/`error`, `notes`, `path`, `workdir`, `stage_dirs`, and `segments`, with exactly one of `outcome` or `error` and closed strict decoding.
decision: fan-in asks lint's existing convergence analysis for its owning parallel rather than reimplementing topology traversal in engine; it consumes only the latest durable branch evidence and delegates the rendered turn to codergen's shared execution path.
friction: strict BranchResult decoding requires array-valued path fields, while Go's natural nil-slice JSON is null; normalize the producer's three required slice fields to empty arrays so its own evidence always round-trips.
decision: the Tractor CLI has only validate, run, and print-schema; validate/run require one explicit file or `--json`, run requires an explicit logs directory, and resume alone restores checkpoint bindings before constructing the real harness backend.
decision: CLI runtime wiring explicitly adds codergen and fan-in while leaving parallel ownership to the engine's real implementation; no placeholder fan-out registration belongs in the CLI.
correction: validation success must not hide warning/info diagnostics; both validate and run report non-error diagnostics to stderr while preserving their existing stdout and error contracts.
decision: provider inference is one exported engine function shared by execution and CLI lint resolution; the CLI system defaults are `gpt-5.6-sol` and high reasoning, with provider deliberately unset so it is inferred through that same path.
correction: cancellable worktree creation requires writing inventory intent before `git worktree add`; completed-run cleanup is idempotent for never-created, manually deleted, and already-removed inventoried paths without repository-global prune.
decision: parallel branches preserve a configured subdirectory workdir by mapping its repository-relative suffix into each worktree; inventory retains the actual worktree root while BranchResult names the mapped execution directory.
decision: resume reuses the same walk, restores the resolved successor and retry visit, and recovers the stage sequence from both checkpoint and disk; adversarial fresh-process proof found only API/message nitpicks, so no extra resume layer is justified.
decision: a stop observed before the first actual handler attempt consumes no execution and writes no checkpoint, preserving the standing retry_visit continuation exactly as Sections 3.1, 3.2, and 3.7 require.
correction: design and slice assignments must stay skeletal and action-oriented; repeated mapping passes without code are waste, so coherent implementers should begin editing immediately and let review expose missing detail.
decision: steering tracks only the active top-level execution; branch stages still emit lifecycle events but cannot replace the parallel parent as the control target.
decision: macOS Unix socket path limits require a short temporary socket only when logs_root/control.sock is too long; advertise that actual path directly and keep the normal case at logs_root/control.sock without a symlink layer.
doc_bug: HarnessBackend trusted an adapter result without independently checking the exact turn schema, so it accepted missing required next and forbidden extra next -> validate the returned object before Outcome conversion.
decision: `branches.json` is engine-authored evidence, so ordinary JSON decoding plus graph/branch identity checks is sufficient; a second strict wire codec duplicated invariants already guaranteed by lint and construction and was removed after final review.
friction: an HTTP response body cannot prove a bodyless status code, and a resume narrative cannot prove process reconstruction -> preserve status text, PID, command line, and exit status as primary live-proof artifacts.
friction: the earlier inherited-Git-environment incident also left the root repository configured as bare with a test-only user identity even after refs/index recovery -> final cleanup must audit and restore repository config, not only branch and worktree contents.
