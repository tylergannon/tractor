decision: `docs/spec.md` copied byte-for-byte from attractor commit e3e0c6a on `origin/codex/critique-spec-repairs`; it is the implementation authority for this task.
decision: keep the manual orchestration skill repo-local at `skills/orchestrate-attractor-loops` so fresh Tractor agents can use the same phase gates and proof discipline.
friction: initial spec archive ran against Tractor instead of the sibling Attractor Git database, and `lefthook install` created a default config -> archive with `git -C /Users/tyler/src/attractor` and replace the generated file with the copied Go-project config before installing.
decision: use the generic Go hook configuration copied from `/Users/tyler/src/codex-autoupdate/lefthook.yml`; it formats with goimports/modernize and runs golangci-lint plus all Go tests.
decision: the augmented objective requires coherent, individually validated feature chunks and directs the design to reuse the Roasbeef Claude Agent SDK choice plus Codex app-server from `/Users/tyler/src/attractor`.
decision: strongly prefer generating and consuming the narrow set of Codex app-server protocol models from the installed CLI schema; avoid generating the entire protocol or leaving generated types unused.
discovery: Claude Code 2.1.206 is installed, but `claude auth status` reports logged out and a real Fable turn fails because the OAuth session expired. The available Doppler token cannot access `tyler-attractor`, so design review and Claude live proof require the user to restore Claude Code authentication.
discovery: Codex 0.147.0 exposes `app-server generate-json-schema`; `github.com/atombender/go-jsonschema` can generate Go models from selected method schemas. Claude uses `github.com/roasbeef/claude-agent-sdk-go`, so there is no analogous Claude app-server client to generate.
friction: one-pass `go-jsonschema` generation across the selected current Codex params, responses, and notifications fails on an empty `anyOf` nested under `TurnErrorCodexErrorInfo` and reports duplicate shared type names. The design must either generate a narrower compatible subset, preprocess the upstream schema deterministically, or choose a different generator; copying a large unused generated surface is not acceptable.
