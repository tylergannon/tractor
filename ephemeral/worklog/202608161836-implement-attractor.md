decision: `docs/spec.md` copied byte-for-byte from attractor commit e3e0c6a on `origin/codex/critique-spec-repairs`; it is the implementation authority for this task.
decision: keep the manual orchestration skill repo-local at `skills/orchestrate-attractor-loops` so fresh Tractor agents can use the same phase gates and proof discipline.
friction: initial spec archive ran against Tractor instead of the sibling Attractor Git database, and `lefthook install` created a default config -> archive with `git -C /Users/tyler/src/attractor` and replace the generated file with the copied Go-project config before installing.
decision: use the generic Go hook configuration copied from `/Users/tyler/src/codex-autoupdate/lefthook.yml`; it formats with goimports/modernize and runs golangci-lint plus all Go tests.
decision: the augmented objective requires coherent, individually validated feature chunks and directs the design to reuse the Roasbeef Claude Agent SDK choice plus Codex app-server from `/Users/tyler/src/attractor`.
decision: strongly prefer generating and consuming the narrow set of Codex app-server protocol models from the installed CLI schema; avoid generating the entire protocol or leaving generated types unused.
discovery: Claude Code 2.1.206 is installed, but `claude auth status` reports logged out and a real Fable turn fails because the OAuth session expired. The available Doppler token cannot access `tyler-attractor`, so design review and Claude live proof require the user to restore Claude Code authentication.
discovery: Codex 0.147.0 exposes `app-server generate-json-schema`; `github.com/atombender/go-jsonschema` can generate Go models from selected method schemas. Claude uses `github.com/roasbeef/claude-agent-sdk-go`, so there is no analogous Claude app-server client to generate.
friction: one-pass `go-jsonschema` generation across the selected current Codex params, responses, and notifications fails on an empty `anyOf` nested under `TurnErrorCodexErrorInfo` and reports duplicate shared type names. The design must either generate a narrower compatible subset, preprocess the upstream schema deterministically, or choose a different generator; copying a large unused generated surface is not acceptable.
discovery: Claude Code authentication was restored on 2026-08-16; `claude auth status` reports first-party Max login and a real `claude -p --model fable` turn returned `READY`.
review: Fable design round 01 session `b5f37476-661e-41fc-8e99-39afaf9123ed` reproduced that Codex `thread/start` does not persist a resumable rollout before the first turn and Claude Code does not persist or emit init for an empty `--session-id`. Both adapters therefore need transient fresh-session state that materializes on first `RunTurn`.
review: round 01 also requires Claude steering to start with the SDK's non-destructive `Stream.Send` path and removal of duplicated backend serialization, model-list discovery, manual external-ref guards, forbidden-import policing, extra CLI output routing, and crash-recovery machinery unrelated to checkpoint sequence continuity.
review: Fable design round 02 in the same session live-confirmed the retained Codex first-turn lifecycle, including resume after an interrupted first turn. It found that `thread/compact/start` only acknowledges start; completion arrives through `ContextCompaction` item completion or `ContextCompactedNotification`, so `Compact` must await that out-of-band boundary.
review: round 02 also requires concrete adapter cleanup for retained fresh Codex processes, honest best-effort semantics for Claude `Stream.Send`, stderr-based terminal classification for Claude's unknown-session exit, and removal of the last checkpoint-only index rescan from this initial slice.
review: Fable design round 03 reproduced that reusing Claude `--session-id` after materialization fails with “already in use”; the fresh marker must be removed immediately when Claude reports the session ID, not only after a terminal result, so interrupted/timeout/error follow-ups use `--resume`.
review: round 03 also requires one explicit category rule for Codex failures before the first rollout, static cleanup from the two conformance factory arms, characterization-before-implementation of the actual Codex compaction signal, and stderr-only unknown-session classification because the pinned Claude SDK does not expose child exit status.
correction: the user rejected highly specific design/requirements artifacts and any code in them. Rewrite the project design as a compact record of choices, empirical constraints, sequencing, and proof gates; move concrete APIs, wire details, and tests into focused per-chunk design/implementation loops only when needed.
review: Fable design round 04 accepted the concise code-free rewrite but live-proved that Codex creates resumable history once `turn/start` reaches the server, even if the process is killed during that first turn. Remove the invented pre-materialization category override and preserve cause-based Appendix D categories; characterize only the reach-server boundary.
review: round 04 also restores fixed in-harness defaults (`gpt-5.6-sol`/medium and `fable`/medium), Claude-first caller detection, explicit dependency pins as design choices, and exact mandatory event/error assertions in the concise proof gate.
review: Fable design round 05 live-proved that policy-free Codex tool use only worked because of ambient machine config. Record explicit unattended policies: Codex approval never plus danger-full-access, Claude bypassPermissions, and terminal refusal of unexpected approval requests.
review: round 05 also restores the exact steering event/workspace assertions and active/unknown compaction assertions, names the schema validator pin, keeps authentication external to the native CLIs, and includes per-turn timeout in the shared contract choice.
correction: the manager prolonged the project-level design review far beyond its value despite the user's explicit warning not to over-engineer. Stop the design loop after the concise code-free decisions artifact and move immediately to implementation; any remaining local uncertainty is resolved while implementing its chunk, not through more global design rounds.
decision: Chunk 1 exposes the spec's backend and adapter contracts in package `harness`; adapters must use the shared input validators before native activity and `ResultValidator` to parse one JSON object and validate the exact caller schema locally, returning terminal categorized errors for invalid input or output.
decision: Chunk 2 needs no cancellation contract expansion: `RunTurnInput.Timeout` leaves timeout enforcement with each adapter, while concurrent `Interrupt` calls let the backend stop all live turns without wrapping synchronous `RunTurn` in goroutines.
decision: `current.jsonl` must return to the globally most recently started segment after concurrent turns drain, even when that segment finished before an older still-live turn; retain the latest segment separately from the live-turn map.
discovery: Claude Code 2.1.233 acknowledges a locally minted session ID in `system/init` before the native transcript is durably resumable; the first later matching-ID native message is the observed materialization boundary, after which process loss and interruption preserve resume.
discovery: the pinned Roasbeef SDK's `Stream.Send` delivers non-destructive active-turn steering, but Claude emits no inbound echo; the adapter must emit the normalized steering `user` event when the send is accepted.
discovery: native Claude `/compact` completes at the terminal status message carrying `compact_result` or `compact_error`; the later compact-boundary and result messages close the slash-command turn but do not define completion.
discovery: Codex 0.147.0 rejects native structured-output schemas unless the top-level `required` array includes every declared property; keep caller schemas exact, surface that provider rejection as terminal, and ensure Tractor's own CLI Outcome schema meets the native strictness rule.
discovery: a real Codex CLI turn created the requested file from runtime-random workspace data, and a second CLI process resumed the returned thread and reproduced the value from native history after both source and result files were removed from the workdir.
discovery: the pinned Claude SDK exposes `WithOutputFormat` but does not translate it into a CLI flag; exact structured output works by supplying the caller schema through `WithExtraArgs` as Claude Code's native `--json-schema` value.
discovery: Claude Code's raw tool-result user message carries `tool_use_id`, but this pinned SDK drops that content-block field while retaining `tool_use_result`; the adapter pairs each retained result with the native assistant tool-call order so public transcripts contain matched call/result events.
friction: unknown Claude resume exits before SDK control initialization, leaving the pinned SDK blocked waiting for initialize despite the native process exit. Race SDK connect against the native `No conversation found` stderr signal, cancel initialization, and classify that provider failure terminal; the real unknown-session probe then returned in 1.26 seconds with no task activity.
proof: caller-aware `agent` selected Claude Code 2.1.233 / Fable from a Codex caller, completed a tool-backed fresh workspace turn, resumed the same native session from two later CLI processes using conversation-only values, and emitted matched tool call/result pairs; evidence is in `ephemeral/projects/tractor/claude-live-proof.md`.
discovery: Claude Code reports a nonexistent model as an `AssistantMessage` with `Error=model_not_found` before its terminal result; classify that native API-error field before projecting assistant events so invalid-model conformance has no task activity.
proof: the single public-interface conformance controller passed all seven Section 11.13 scenarios against Codex 0.147.0 / gpt-5.6-sol and Claude Code 2.1.233 / Fable; concise per-scenario evidence is under `ephemeral/projects/tractor/conformance/`.
