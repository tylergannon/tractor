correction: Tractor followed the copied spec's explicit removal of the in-graph supervisor, but the user expects the upstream manager node to remain a core capability. Restore a real typed manager node and prove it; do not substitute an ordinary codergen workaround.
decision: Keep the manager implementation aligned with Tractor's existing architecture: a typed JSON node launches a child JSON pipeline, observes the child's durable run surfaces, and steers through the child's advertised /steer control socket.
decision: The user removed manager-node validation from this task and chose to retain the current controversial omission for now. Do not edit the spec or implement a manager node in this pass.
decision: Live examples must be self-verifying: the steering graph ends with a tool assertion over a file named only by the external steering message, and the fan-in graph ends with a tool assertion over content available only from two isolated branch worktrees.
friction: The live parallel proof exposed that timeline.jsonl records event order and durations but no absolute timestamp, making correlation with steering audits and harness events harder during an ongoing run. Stamp every lifecycle event at append time.
review: Independent round 01 reproduced both live claims. Fix its material findings by correcting the example index, documenting the committed-HEAD precondition, and attributing live branch stage events with branch ID and workdir; also preserve caller timestamps without mutating event maps and remove vacuous branch-local isolation checks.
review: Round 02 independently reran both examples at commit 1e9a0c9 and confirmed the functional fixes, but correctly rejected the older retained run as proof of the edited HEAD. Preserve the reviewer's already-paid-for primary artifacts, make the workdir assertion actually reject a missing field, and add a main-workspace no-leak assertion. Decline an unreachable nil-event guard because every internal timeline call constructs a non-nil literal.
review: Round 03 confirmed the current graph live but found the imported proof omitted invocation, exit status, and binary provenance. Before the final captured runs, advertise the main workspace in manifest.json, decouple the branch-attribution test from worktree naming, and link examples from the root README; then freeze implementation and capture both examples with complete process evidence.
proof: Frozen commit dbbf5cba726aa16747e33f57380d7688cfcd696f passed one final fully captured steering run and one fully captured parallel run. The parallel evidence includes a live snapshot taken before either branch completed; both manifests include the main workspace; no runtime or example edits followed.
review: Round 04 found no software defect; its one material evidence finding was that the disposable workspace Git metadata had been moved without recording the post-run repository state. Reattach the untouched metadata, capture status/worktree/HEAD/stashes, move it back out to avoid an embedded-repository gitlink, and document both the transformation and live-snapshot capture command. Leave timeout tuning and pre-existing checkpoint key encoding as nits.
review: Round 05 correctly found that the report overstated when the retained observation snapshot was captured. This supersedes the earlier snapshot description: the timestamps prove it was captured during the live fan-in turn after both branches completed, not while the branches were still running; correct only that claim. Decline a new steer/observe CLI and duplicate repository-state capture because the reviewer classified both as ergonomics/evidence nitpicks outside the requested implementation.
friction: Whole-branch `git diff --check` reports one trailing blank line in an earlier raw `response.md` proof artifact. Preserve the literal provider output instead of cosmetically rewriting evidence; the final consensus commit itself passes `git diff --check`.
