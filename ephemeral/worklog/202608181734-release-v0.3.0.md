decision: Prepare v0.3.0 from a fresh remote checkout because the existing root checkout belongs to an interrupted release task that must remain untouched.
friction: The repository sandbox blocks the default Go build cache, local TCP/Unix socket binding, and process inspection -> use a task-local GOCACHE for compilation-only checks and scoped host execution for the race suite and preview verification.
