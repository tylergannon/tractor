decision: Store the release workflow under `.agents/skills/release-tractor` so Codex discovers it for every Tractor workspace, and make `AGENTS.md` require it for all release-related requests.
decision: For routine releases, bump `.codex-plugin/plugin.json`, keep the marketplace entry tracking `main`, then upgrade the marketplace and reinstall; the marketplace catalog itself does not need a cosmetic version edit.
correction: Release work must encode the Codex marketplace refresh and installed-version proof instead of relying on the next agent to remember it.
