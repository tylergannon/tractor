#!/bin/sh
set -eu

plugin_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
plugin_key=$(printf '%s' "$plugin_root" | cksum)
plugin_key=${plugin_key%% *}
cache_root=${XDG_CACHE_HOME:-"$HOME/.cache"}/tractor/codex-plugin/$plugin_key
binary=$cache_root/tractor
candidate=$cache_root/tractor.$$

mkdir -p "$cache_root"
trap 'rm -f "$candidate"' EXIT HUP INT TERM

cd "$plugin_root"
go build -o "$candidate" ./cmd/tractor
mv -f "$candidate" "$binary"
trap - EXIT HUP INT TERM

exec "$binary" mcp
